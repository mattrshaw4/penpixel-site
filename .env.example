# Copy to .env for local dev. NEVER commit the real .env.
# These are only needed once the contact form Worker is added (later phase).
# Real values live in the Cloudflare dashboard env vars, not in the repo.
TURNSTILE_SECRET_KEY=
CONTACT_FORWARD_EMAIL=
